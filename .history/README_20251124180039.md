# To-Do List - Aplicación PHP con MVC

Una aplicación web sencilla pero robusta para la gestión de tareas, desarrollada en PHP puro siguiendo el patrón de diseño Modelo-Vista-Controlador (MVC). Este proyecto demuestra una estructura de código organizada, manejo de sesiones de usuario y operaciones CRUD básicas contra una base de datos MySQL.

## ✨ Características Principales

- **Autenticación de Usuarios:**
  - Registro de nuevos usuarios.
  - Inicio de sesión (Login) y cierre de sesión (Logout).
  - Rutas protegidas que requieren autenticación.
- **Gestión de Tareas (CRUD):**
  - **Crear:** Añadir nuevas tareas con título, descripción y nivel de prioridad.
  - **Leer:** Visualizar la lista completa de tareas.
  - **Actualizar:** Marcar tareas como "Finalizadas".
  - **Eliminar:** Borrar tareas de la lista.
- **Diseño Responsivo:** Interfaz de usuario limpia y funcional gracias a Bootstrap.
- **Arquitectura MVC:**
  - **Modelo:** Encargado de la lógica de negocio y la interacción con la base de datos (usando PDO).
  - **Vista:** Presenta los datos al usuario a través de plantillas PHTML.
  - **Controlador:** Actúa como intermediario, gestionando las solicitudes del usuario.
- **Enrutador Front-Controller:** Un único punto de entrada (`index.php` con `router.php`) que dirige todas las peticiones a los controladores correspondientes.

## 🛠️ Tecnologías Utilizadas

- **Backend:** PHP 7+
- **Base de Datos:** MySQL
- **Frontend:** HTML, CSS, Bootstrap 5
- **Servidor Web:** Apache (generalmente a través de XAMPP, WAMP, etc.)

## 📂 Estructura del Proyecto

El proyecto está organizado siguiendo las mejores prácticas de la arquitectura MVC para mantener el código desacoplado y fácil de mantener.

```
/
├── app/
│   ├── controllers/      # Controladores (lógica de la aplicación)
│   │   ├── auth.controller.php
│   │   ├── task.controller.php
│   │   └── about.controller.php
│   ├── models/           # Modelos (interacción con la base de datos)
│   │   ├── user.model.php
│   │   └── task.model.php
│   ├── views/            # Vistas (lógica de presentación)
│   │   ├── auth.view.php
│   │   ├── task.view.php
│   │   └── about.view.php
│   └── helpers/          # Clases de ayuda (ej. manejo de sesión)
│       └── auth.helper.php
├── css/
│   └── style.css         # Estilos personalizados
├── templates/            # Plantillas HTML (archivos .phtml)
│   ├── header.phtml
│   ├── footer.phtml
│   ├── taskList.phtml
│   └── ...
├── .htaccess             # Configuración de Apache para URLs amigables
├── router.php            # Enrutador principal
└── index.php             # Punto de entrada de la aplicación
```

## 🚀 Instalación y Puesta en Marcha

Sigue estos pasos para ejecutar el proyecto en tu entorno local.

### 1. Prerrequisitos

- Un entorno de desarrollo local como XAMPP o similar, que incluya Apache, MySQL y PHP.

### 2. Configuración de la Base de Datos

1.  Inicia el servicio de MySQL desde tu panel de control (ej. XAMPP).
2.  Abre phpMyAdmin (`http://localhost/phpmyadmin`).
3.  Crea una nueva base de datos llamada `db_tareas`.
4.  Ejecuta las siguientes sentencias SQL en la pestaña "SQL" de tu nueva base de datos para crear las tablas necesarias:

```sql
-- Tabla para los usuarios
CREATE TABLE `usuarios` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `email` varchar(255) NOT NULL,
  `password` varchar(255) NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `email` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- Tabla para las tareas
CREATE TABLE `tareas` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `titulo` varchar(255) NOT NULL,
  `descripcion` text DEFAULT NULL,
  `prioridad` int(11) NOT NULL,
  `finalizada` tinyint(1) NOT NULL DEFAULT 0,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 3. Configuración del Proyecto

1.  Clona o descarga este repositorio en la carpeta `htdocs` de tu instalación de XAMPP.
2.  Asegúrate de que las credenciales de la base de datos en los archivos del modelo (`app/models/task.model.php` y `app/models/user.model.php`) coincidan con tu configuración local. Por defecto, están configuradas para:
    - **Usuario:** `root`
    - **Contraseña:** (vacía)
3.  Abre tu navegador y accede a la carpeta del proyecto. Por ejemplo: `http://localhost/todoList/`.

¡Listo! Ahora puedes registrar un usuario y empezar a gestionar tus tareas.
